# Environment Variables for Production

# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your_super_secure_jwt_secret_here

# MongoDB Connection String
# For Render + MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database
# For Railway: Use the MongoDB connection string provided by Railway
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/heart_rate_db

# Node Environment
NODE_ENV=production

# Port (automatically set by Render)
PORT=10000

# Optional: Python path (usually auto-detected)
PYTHONPATH=/usr/local/bin/python3